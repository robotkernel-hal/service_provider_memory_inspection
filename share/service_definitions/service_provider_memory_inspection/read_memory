services:
  - name: service_provider_memory_inspection/read_memory
    request:
      - { name: address, type: uint64_t }
      - { name: length, type: uint32_t }
    response:
      - { name: data, type: uint8_t, array: true }
      - { name: error_message, type: string, default: "no error" }
