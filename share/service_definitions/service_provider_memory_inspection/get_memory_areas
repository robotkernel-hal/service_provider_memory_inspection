datatypes:
  service_provider_memory_inspection/memory_area:
    fields: 
      address: { type: uint64_t }
      length: { type: uint32_t } 
services:
  service_provider_memory_inspection/get_memory_areas:
    response:
      areas: { type: service_provider_memory_inspection/memory_area, array: true }
      error_message: { type: string, default: "no error" }

